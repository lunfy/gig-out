<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>

  <body>

    <header>
        <%- include('../partials/header') %> 
    </header>

    <main>
      <div id="art">
        <article class="main-art">
          <h1 class="title is-underlined latest">Latest Updates</h1>
          <% if (indGig) { %>
              <section class="section posting mb-5">
                <h2 class="title"><a href='/gigs/<%= indGig._id %>'> <%= indGig.title %> </a></h2>
                <p class="subtitle"><strong>Poster:</strong> <%= indGig.authorFname %> <%= indGig.authorLname %> </p>
                <p><strong>Work Date:</strong> <%= indGig.workDate.getDay()+' '+indGig.workDate.toLocaleString('default', {month: 'short'})+' '+indGig.workDate.getFullYear() %></p><p><strong>Gig Type:</strong> <%= indGig.type %></p>
                <p><strong>Work Area:</strong> <%= indGig.workArea %></p>
                <p><strong>Job Description:</strong> <%= indGig.messageBody%></p>
                <p><strong>Payment via:</strong> <%= indGig.paymentBy %></p>
                <p><strong>Amount:</strong> $<%= indGig.amount %></p>
                <br>
                <p class="p-date"><strong>Last Updated:</strong> <%= indGig.lastUpdated %></p>
              </section>
            <% } %>
        </article>
      </div>
    </main>

    <footer>
      <!-- Insert footer partials here -->
    </footer>

    <!-- possible some script partials here -->
    <script> <%- include('../../public/scripts/navbar.js') %> </script>

  </body>
</html>